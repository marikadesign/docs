# 出力ルール

- CSSはHTML内に埋め込み、1ファイルで完結させること
- 日本語で自然なテキストにすること
- 絵文字は一切使用しないこと（例外なく禁止）
- 外部画像の検索・取得は禁止
- 既存ファイルは上書きせず、連番で新規作成すること
- HTMLのファイルサイズは10MB以内に収めること

# デザイン方針

- テンプレートではなく、視覚的な強弱とメリハリを持たせること
- source/researchを参照し、構造と雰囲気を設計のベースとする（直接使用は禁止）
- source/imageの画像は必ず使用すること
- 画像は事前に圧縮し、Base64形式でHTML内に埋め込むこと（外部参照・パス参照は禁止）
- 画像は1枚あたり最大300KB以内とすること
- 同一画像の複数使用は禁止とする
- 画像は役割を明確にし、ファーストビューでは補助として扱うこと

- セクションごとの役割を明確にし、一貫した構成で設計すること
- ファーストビューはテキストを主役とすること
- ファーストビューは適切な情報密度（1メッセージ）に抑えること
- ファーストビューは直感的に理解できる構成とし、過剰な説明を避けること